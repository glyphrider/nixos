# Host-specific system config for `nixie`, layered on top of the shared
# ../../configuration.nix. Nothing host-specific yet beyond hostname and
# hardware-configuration.nix (handled in flake.nix).

{ ... }:

{ }
