# Host-specific home-manager config for `beast`, layered on top of the
# shared ../../home.nix. Nothing host-specific yet beyond the monitor list
# (handled in flake.nix). In particular, no edifier-bluetooth-autoconnect
# service here — beast's speakers are wired via TOSLink, not Bluetooth.

{ ... }:

{ }
